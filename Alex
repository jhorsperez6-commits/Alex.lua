local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MenuAlexPro"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

-- ========== CARGA ==========
local LoadingFrame = Instance.new("Frame")
LoadingFrame.Size = UDim2.new(1, 0, 1, 0)
LoadingFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 14)
LoadingFrame.Parent = ScreenGui

local LoadTitle = Instance.new("TextLabel")
LoadTitle.Size = UDim2.new(1, 0, 0, 36)
LoadTitle.Position = UDim2.new(0, 0, 0.34, 0)
LoadTitle.BackgroundTransparency = 1
LoadTitle.RichText = true
LoadTitle.Text = '<font color="rgb(255,55,55)">TikTok</font> <font color="rgb(0,185,255)">alex@11</font>'
LoadTitle.Font = Enum.Font.GothamBlack
LoadTitle.TextSize = 24
LoadTitle.Parent = LoadingFrame

local Spinner = Instance.new("ImageLabel")
Spinner.Size = UDim2.new(0, 64, 0, 64)
Spinner.Position = UDim2.new(0.5, -32, 0.43, 0)
Spinner.BackgroundTransparency = 1
Spinner.Image = "rbxassetid://4965945816"
Spinner.ImageColor3 = Color3.fromRGB(0, 190, 255)
Spinner.Parent = LoadingFrame

local Percent = Instance.new("TextLabel")
Percent.Size = UDim2.new(1, 0, 0, 28)
Percent.Position = UDim2.new(0, 0, 0.56, 0)
Percent.BackgroundTransparency = 1
Percent.Text = "0%"
Percent.TextColor3 = Color3.fromRGB(240, 240, 240)
Percent.Font = Enum.Font.GothamBold
Percent.TextSize = 20
Percent.Parent = LoadingFrame

local angle = 0
local spinConn = RunService.RenderStepped:Connect(function()
	angle += 7
	Spinner.Rotation = angle
end)

local OpenButton = Instance.new("TextButton")
OpenButton.Size = UDim2.new(0, 150, 0, 38)
OpenButton.Position = UDim2.new(0, 14, 0, 14)
OpenButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
OpenButton.Text = "Abrir Menú"
OpenButton.TextColor3 = Color3.fromRGB(15, 15, 15)
OpenButton.Font = Enum.Font.GothamBold
OpenButton.TextSize = 14
OpenButton.Parent = ScreenGui
OpenButton.Visible = false
Instance.new("UICorner", OpenButton).CornerRadius = UDim.new(0, 10)

task.spawn(function()
	for i = 0, 100 do
		Percent.Text = i .. "%"
		task.wait(0.015)
	end
	task.wait(0.25)
	spinConn:Disconnect()
	TweenService:Create(LoadingFrame, TweenInfo.new(0.35), {BackgroundTransparency = 1}):Play()
	TweenService:Create(LoadTitle, TweenInfo.new(0.35), {TextTransparency = 1}):Play()
	TweenService:Create(Spinner, TweenInfo.new(0.35), {ImageTransparency = 1}):Play()
	TweenService:Create(Percent, TweenInfo.new(0.35), {TextTransparency = 1}):Play()
	task.wait(0.4)
	LoadingFrame:Destroy()
	OpenButton.Visible = true
end)

-- ========== MENÚ PRINCIPAL ==========
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 22)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(0, 170, 255)
UIStroke.Thickness = 1.5
UIStroke.Parent = MainFrame

local TitleFrame = Instance.new("Frame")
TitleFrame.Size = UDim2.new(1, 0, 0, 44)
TitleFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
TitleFrame.Parent = MainFrame
Instance.new("UICorner", TitleFrame).CornerRadius = UDim.new(0, 14)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 1, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.RichText = true
TitleLabel.Text = '<font color="rgb(255,55,55)">TikTok</font> <font color="rgb(0,185,255)">alex@11</font>'
TitleLabel.Font = Enum.Font.GothamBlack
TitleLabel.TextSize = 20
TitleLabel.Parent = TitleFrame

local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -14, 0, 32)
TabBar.Position = UDim2.new(0, 7, 0, 50)
TabBar.BackgroundColor3 = Color3.fromRGB(26, 26, 34)
TabBar.Parent = MainFrame
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 8)

local tabs = {"Aimbot", "Mover", "Visual", "TP", "Servidor"}
local tabButtons = {}
local pages = {}
local currentPage = nil

local function createPage(name)
	local page = Instance.new("Frame")
	page.Size = UDim2.new(1, -14, 1, -92)
	page.Position = UDim2.new(0, 7, 0, 88)
	page.BackgroundTransparency = 1
	page.Visible = false
	page.Parent = MainFrame
	pages[name] = page
	return page
end

local function createToggle(parent, name, yPos, callback)
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, 0, 0, 34)
	container.Position = UDim2.new(0, 0, 0, yPos)
	container.BackgroundColor3 = Color3.fromRGB(24, 24, 32)
	container.Parent = parent
	Instance.new("UICorner", container).CornerRadius = UDim.new(0, 8)

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.62, 0, 1, 0)
	label.Position = UDim2.new(0.04, 0, 0, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = Color3.fromRGB(230, 230, 230)
	label.Font = Enum.Font.GothamMedium
	label.TextSize = 13
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = container

	local switch = Instance.new("TextButton")
	switch.Size = UDim2.new(0, 50, 0, 24)
	switch.Position = UDim2.new(1, -58, 0.5, -12)
	switch.BackgroundColor3 = Color3.fromRGB(190, 45, 45)
	switch.Text = "OFF"
	switch.TextColor3 = Color3.new(1,1,1)
	switch.Font = Enum.Font.GothamBold
	switch.TextSize = 11
	switch.Parent = container
	Instance.new("UICorner", switch).CornerRadius = UDim.new(0, 11)

	local enabled = false
	switch.MouseButton1Click:Connect(function()
		enabled = not enabled
		local goal = enabled and Color3.fromRGB(35, 180, 75) or Color3.fromRGB(190, 45, 45)
		TweenService:Create(switch, TweenInfo.new(0.2), {BackgroundColor3 = goal}):Play()
		switch.Text = enabled and "ON" or "OFF"
		if callback then callback(enabled) end
	end)
end

local pageAimbot = createPage("Aimbot")
local pageMove = createPage("Mover")
local pageVisual = createPage("Visual")
local pageTP = createPage("TP")
local pageServer = createPage("Servidor")

-- Variables
local aimbotEnabled = false
local hitboxEnabled = false
local currentSpeed = 16
local infiniteJumpEnabled = false
local noclipEnabled = false
local espEnabled = false
local hitboxColor = Color3.fromRGB(255, 40, 40)
local hitboxTransparency = 0.7
local hitboxSize = 7
local hitboxDistance = 300
local autoStickPlayer = nil
local originalSizes = {}
local espBoxes = {}
local espTracers = {}

local function isEnemy(player)
	if player == LocalPlayer then return false end
	if LocalPlayer.Team and player.Team then
		return player.Team ~= LocalPlayer.Team
	end
	return true
end

-- === AIMBOT ===
createToggle(pageAimbot, "Aimbot", 2, function(v) aimbotEnabled = v end)
createToggle(pageAimbot, "Hitbox", 42, function(v)
	hitboxEnabled = v
	if not v then
		for player, data in pairs(originalSizes) do
			pcall(function()
				local char = player.Character
				if char and char:FindFirstChild("HumanoidRootPart") and data.root then
					char.HumanoidRootPart.Size = data.root
					char.HumanoidRootPart.Transparency = data.rootTrans or 0
					char.HumanoidRootPart.Color = Color3.fromRGB(163, 162, 165)
					char.HumanoidRootPart.Material = Enum.Material.Plastic
				end
			end)
		end
		originalSizes = {}
	end
end)

local ColorLabel = Instance.new("TextLabel")
ColorLabel.Size = UDim2.new(1, 0, 0, 18)
ColorLabel.Position = UDim2.new(0, 0, 0, 84)
ColorLabel.BackgroundTransparency = 1
ColorLabel.Text = "Color Hitbox"
ColorLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
ColorLabel.Font = Enum.Font.Gotham
ColorLabel.TextSize = 12
ColorLabel.Parent = pageAimbot

local colorOptions = {
	{color = Color3.fromRGB(255, 40, 40), transparency = 0.65},
	{color = Color3.fromRGB(40, 255, 90), transparency = 0.65},
	{color = Color3.fromRGB(180, 50, 255), transparency = 0.65},
	{color = Color3.fromRGB(0, 150, 255), transparency = 0.65},
	{color = Color3.fromRGB(255, 255, 255), transparency = 0.9},
}

local selectedColorBtn = nil
for i, option in ipairs(colorOptions) do
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 28, 0, 28)
	btn.Position = UDim2.new(0, 6 + (i-1)*42, 0, 105)
	btn.BackgroundColor3 = option.color
	btn.Text = ""
	btn.Parent = pageAimbot
	Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)

	local stroke = Instance.new("UIStroke")
	stroke.Color = Color3.fromRGB(255, 255, 255)
	stroke.Thickness = 0
	stroke.Parent = btn

	btn.MouseButton1Click:Connect(function()
		hitboxColor = option.color
		hitboxTransparency = option.transparency
		if selectedColorBtn then
			selectedColorBtn:FindFirstChildOfClass("UIStroke").Thickness = 0
		end
		stroke.Thickness = 2
		selectedColorBtn = btn
	end)
	if i == 1 then
		stroke.Thickness = 2
		selectedColorBtn = btn
	end
end

local SizeLabel = Instance.new("TextLabel")
SizeLabel.Size = UDim2.new(1, 0, 0, 18)
SizeLabel.Position = UDim2.new(0, 0, 0, 142)
SizeLabel.BackgroundTransparency = 1
SizeLabel.Text = "Tamaño Hitbox"
SizeLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
SizeLabel.Font = Enum.Font.Gotham
SizeLabel.TextSize = 12
SizeLabel.Parent = pageAimbot

local SizeFrame = Instance.new("Frame")
SizeFrame.Size = UDim2.new(1, 0, 0, 32)
SizeFrame.Position = UDim2.new(0, 0, 0, 162)
SizeFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
SizeFrame.Parent = pageAimbot
Instance.new("UICorner", SizeFrame).CornerRadius = UDim.new(0, 8)

local SizeValue = Instance.new("TextLabel")
SizeValue.Size = UDim2.new(0.4, 0, 1, 0)
SizeValue.Position = UDim2.new(0.3, 0, 0, 0)
SizeValue.BackgroundTransparency = 1
SizeValue.Text = "7"
SizeValue.TextColor3 = Color3.fromRGB(255, 255, 255)
SizeValue.Font = Enum.Font.GothamBold
SizeValue.TextSize = 16
SizeValue.Parent = SizeFrame

local MinusBtn = Instance.new("TextButton")
MinusBtn.Size = UDim2.new(0, 36, 0, 24)
MinusBtn.Position = UDim2.new(0, 6, 0.5, -12)
MinusBtn.BackgroundColor3 = Color3.fromRGB(190, 50, 50)
MinusBtn.Text = "−"
MinusBtn.TextColor3 = Color3.new(1,1,1)
MinusBtn.Font = Enum.Font.GothamBold
MinusBtn.TextSize = 18
MinusBtn.Parent = SizeFrame
Instance.new("UICorner", MinusBtn).CornerRadius = UDim.new(0, 6)

local PlusBtn = Instance.new("TextButton")
PlusBtn.Size = UDim2.new(0, 36, 0, 24)
PlusBtn.Position = UDim2.new(1, -42, 0.5, -12)
PlusBtn.BackgroundColor3 = Color3.fromRGB(40, 170, 70)
PlusBtn.Text = "+"
PlusBtn.TextColor3 = Color3.new(1,1,1)
PlusBtn.Font = Enum.Font.GothamBold
PlusBtn.TextSize = 18
PlusBtn.Parent = SizeFrame
Instance.new("UICorner", PlusBtn).CornerRadius = UDim.new(0, 6)

MinusBtn.MouseButton1Click:Connect(function()
	hitboxSize = math.clamp(hitboxSize - 1, 1, 40)
	SizeValue.Text = tostring(hitboxSize)
end)
PlusBtn.MouseButton1Click:Connect(function()
	hitboxSize = math.clamp(hitboxSize + 1, 1, 40)
	SizeValue.Text = tostring(hitboxSize)
end)

-- === MOVER ===
createToggle(pageMove, "Velocidad", 2, function() end)

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(1, 0, 0, 32)
SpeedBox.Position = UDim2.new(0, 0, 0, 42)
SpeedBox.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
SpeedBox.Text = "16"
SpeedBox.PlaceholderText = "1 - 300"
SpeedBox.TextColor3 = Color3.fromRGB(230, 230, 230)
SpeedBox.Font = Enum.Font.Gotham
SpeedBox.TextSize = 13
SpeedBox.Parent = pageMove
Instance.new("UICorner", SpeedBox).CornerRadius = UDim.new(0, 8)

SpeedBox.FocusLost:Connect(function()
	local num = tonumber(SpeedBox.Text)
	if num then
		currentSpeed = math.clamp(num, 1, 300)
		SpeedBox.Text = tostring(currentSpeed)
	end
end)

createToggle(pageMove, "Salto Infinito", 84, function(v) infiniteJumpEnabled = v end)
createToggle(pageMove, "Noclip", 124, function(v) noclipEnabled = v end)

-- === VISUAL ===
createToggle(pageVisual, "ESP", 2, function(v)
	espEnabled = v
	if not v then
		for _, box in pairs(espBoxes) do pcall(function() box:Remove() end) end
		for _, tracer in pairs(espTracers) do pcall(function() tracer:Remove() end) end
		espBoxes = {}
		espTracers = {}
	end
end)

-- === TP ===
local TPButton = Instance.new("TextButton")
TPButton.Size = UDim2.new(1, 0, 0, 34)
TPButton.Position = UDim2.new(0, 0, 0, 6)
TPButton.BackgroundColor3 = Color3.fromRGB(0, 130, 240)
TPButton.Text = "Lista de Enemigos"
TPButton.TextColor3 = Color3.new(1,1,1)
TPButton.Font = Enum.Font.GothamBold
TPButton.TextSize = 13
TPButton.Parent = pageTP
Instance.new("UICorner", TPButton).CornerRadius = UDim.new(0, 8)

local StopTP = Instance.new("TextButton")
StopTP.Size = UDim2.new(1, 0, 0, 34)
StopTP.Position = UDim2.new(0, 0, 0, 48)
StopTP.BackgroundColor3 = Color3.fromRGB(190, 45, 45)
StopTP.Text = "Detener Teleport"
StopTP.TextColor3 = Color3.new(1,1,1)
StopTP.Font = Enum.Font.GothamBold
StopTP.TextSize = 13
StopTP.Parent = pageTP
Instance.new("UICorner", StopTP).CornerRadius = UDim.new(0, 8)

-- === SERVIDOR ===
local HopButton = Instance.new("TextButton")
HopButton.Size = UDim2.new(1, 0, 0, 38)
HopButton.Position = UDim2.new(0, 0, 0, 10)
HopButton.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
HopButton.Text = "🔄 Cambiar de Servidor"
HopButton.TextColor3 = Color3.new(1,1,1)
HopButton.Font = Enum.Font.GothamBold
HopButton.TextSize = 14
HopButton.Parent = pageServer
Instance.new("UICorner", HopButton).CornerRadius = UDim.new(0, 8)

local RejoinButton = Instance.new("TextButton")
RejoinButton.Size = UDim2.new(1, 0, 0, 38)
RejoinButton.Position = UDim2.new(0, 0, 0, 58)
RejoinButton.BackgroundColor3 = Color3.fromRGB(0, 130, 240)
RejoinButton.Text = "🔁 Rejoin (Mismo Servidor)"
RejoinButton.TextColor3 = Color3.new(1,1,1)
RejoinButton.Font = Enum.Font.GothamBold
RejoinButton.TextSize = 14
RejoinButton.Parent = pageServer
Instance.new("UICorner", RejoinButton).CornerRadius = UDim.new(0, 8)

local ServerInfo = Instance.new("TextLabel")
ServerInfo.Size = UDim2.new(1, 0, 0, 40)
ServerInfo.Position = UDim2.new(0, 0, 0, 110)
ServerInfo.BackgroundTransparency = 1
ServerInfo.Text = "Jugadores: " .. #Players:GetPlayers() .. "\nPlaceId: " .. game.PlaceId
ServerInfo.TextColor3 = Color3.fromRGB(180, 180, 180)
ServerInfo.Font = Enum.Font.Gotham
ServerInfo.TextSize = 12
ServerInfo.TextYAlignment = Enum.TextYAlignment.Top
ServerInfo.Parent = pageServer

HopButton.MouseButton1Click:Connect(function()
	HopButton.Text = "Buscando servidor..."
	local success = pcall(function()
		local servers = {}
		local req = game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")
		local data = HttpService:JSONDecode(req)
		if data and data.data then
			for _, server in pairs(data.data) do
				if server.playing < server.maxPlayers and server.id ~= game.JobId then
					table.insert(servers, server.id)
				end
			end
		end
		if #servers > 0 then
			TeleportService:TeleportToPlaceInstance(game.PlaceId, servers[math.random(1, #servers)], LocalPlayer)
		else
			TeleportService:Teleport(game.PlaceId)
		end
	end)
	if not success then
		TeleportService:Teleport(game.PlaceId)
	end
end)

RejoinButton.MouseButton1Click:Connect(function()
	TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
end)

-- Tabs
for i, name in ipairs(tabs) do
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0.2, -3, 1, -6)
	btn.Position = UDim2.new((i-1)*0.2, 2, 0, 3)
	btn.BackgroundColor3 = Color3.fromRGB(36, 36, 48)
	btn.Text = name
	btn.TextColor3 = Color3.fromRGB(170, 170, 170)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 11
	btn.Parent = TabBar
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

	btn.MouseButton1Click:Connect(function()
		if currentPage and currentPage ~= pages[name] then
			local old = currentPage
			TweenService:Create(old, TweenInfo.new(0.16), {Position = UDim2.new(-1, 7, 0, 88)}):Play()
			task.delay(0.16, function() old.Visible = false old.Position = UDim2.new(0, 7, 0, 88) end)
		end
		for _, b in pairs(tabButtons) do
			TweenService:Create(b, TweenInfo.new(0.16), {BackgroundColor3 = Color3.fromRGB(36, 36, 48)}):Play()
			b.TextColor3 = Color3.fromRGB(170, 170, 170)
		end
		pages[name].Visible = true
		pages[name].Position = UDim2.new(1, 7, 0, 88)
		TweenService:Create(pages[name], TweenInfo.new(0.2, Enum.EasingStyle.Quint), {Position = UDim2.new(0, 7, 0, 88)}):Play()
		TweenService:Create(btn, TweenInfo.new(0.16), {BackgroundColor3 = Color3.fromRGB(0, 150, 240)}):Play()
		btn.TextColor3 = Color3.new(1,1,1)
		currentPage = pages[name]
	end)
	tabButtons[name] = btn
end

pages["Aimbot"].Visible = true
currentPage = pages["Aimbot"]
tabButtons["Aimbot"].BackgroundColor3 = Color3.fromRGB(0, 150, 240)
tabButtons["Aimbot"].TextColor3 = Color3.new(1,1,1)

-- Lista enemigos
local ListFrame = Instance.new("Frame")
ListFrame.Size = UDim2.new(0, 230, 0, 260)
ListFrame.Position = UDim2.new(0.5, -115, 0.25, 0)
ListFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 22)
ListFrame.Visible = false
ListFrame.Active = true
ListFrame.Draggable = true
ListFrame.Parent = ScreenGui
Instance.new("UICorner", ListFrame).CornerRadius = UDim.new(0, 12)

local ListTitle = Instance.new("TextLabel")
ListTitle.Size = UDim2.new(1, 0, 0, 34)
ListTitle.BackgroundColor3 = Color3.fromRGB(0, 130, 240)
ListTitle.Text = "Elegir Enemigo"
ListTitle.TextColor3 = Color3.new(1,1,1)
ListTitle.Font = Enum.Font.GothamBold
ListTitle.TextSize = 14
ListTitle.Parent = ListFrame
Instance.new("UICorner", ListTitle).CornerRadius = UDim.new(0, 12)

local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(0.92, 0, 0, 208)
Scroll.Position = UDim2.new(0.04, 0, 0.15, 0)
Scroll.BackgroundColor3 = Color3.fromRGB(24, 24, 32)
Scroll.ScrollBarThickness = 3
Scroll.Parent = ListFrame
Instance.new("UICorner", Scroll).CornerRadius = UDim.new(0, 8)

TPButton.MouseButton1Click:Connect(function()
	ListFrame.Visible = true
	Scroll:ClearAllChildren()
	local y = 4
	for _, player in pairs(Players:GetPlayers()) do
		if isEnemy(player) then
			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(0.95, 0, 0, 30)
			btn.Position = UDim2.new(0.025, 0, 0, y)
			btn.BackgroundColor3 = Color3.fromRGB(0, 130, 240)
			btn.Text = player.Name
			btn.TextColor3 = Color3.new(1,1,1)
			btn.Font = Enum.Font.GothamBold
			btn.TextSize = 12
			btn.Parent = Scroll
			Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
			btn.MouseButton1Click:Connect(function()
				autoStickPlayer = player
				ListFrame.Visible = false
			end)
			y = y + 36
		end
	end
	Scroll.CanvasSize = UDim2.new(0, 0, 0, y)
end)

StopTP.MouseButton1Click:Connect(function()
	autoStickPlayer = nil
end)

-- Abrir / Cerrar
local isOpen = false
OpenButton.MouseButton1Click:Connect(function()
	isOpen = not isOpen
	if isOpen then
		MainFrame.Visible = true
		MainFrame.Size = UDim2.new(0, 0, 0, 0)
		MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
		TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back), {
			Size = UDim2.new(0, 280, 0, 360),
			Position = UDim2.new(0.5, -140, 0.14, 0)
		}):Play()
		OpenButton.Text = "Cerrar Menú"
		OpenButton.BackgroundColor3 = Color3.fromRGB(210, 45, 45)
	else
		TweenService:Create(MainFrame, TweenInfo.new(0.2), {
			Size = UDim2.new(0, 0, 0, 0),
			Position = UDim2.new(0.5, 0, 0.5, 0)
		}):Play()
		task.delay(0.2, function() MainFrame.Visible = false end)
		OpenButton.Text = "Abrir Menú"
		OpenButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	end
end)

-- ========== FUNCIONES ==========
local function isVisible(char)
	local cam = workspace.CurrentCamera
	if not cam or not char or not char:FindFirstChild("HumanoidRootPart") then return false end
	local origin = cam.CFrame.Position
	local target = char.HumanoidRootPart.Position + Vector3.new(0, 1.5, 0)
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Exclude
	params.FilterDescendantsInstances = {LocalPlayer.Character, char}
	local result = workspace:Raycast(origin, target - origin, params)
	return result == nil or (result.Instance and result.Instance:IsDescendantOf(char))
end

local function getClosestEnemy()
	local closest, shortest = nil, math.huge
	for _, player in pairs(Players:GetPlayers()) do
		if isEnemy(player) and player.Character and player.Character:FindFirstChild("Head") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
			local pos = Camera:WorldToViewportPoint(player.Character.Head.Position)
			local dist = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
			if dist < shortest then
				shortest = dist
				closest = player
			end
		end
	end
	return closest
end

RunService.RenderStepped:Connect(function()
	if aimbotEnabled then
		local target = getClosestEnemy()
		if target and target.Character and target.Character:FindFirstChild("Head") then
			local goal = CFrame.new(Camera.CFrame.Position, target.Character.Head.Position)
			Camera.CFrame = Camera.CFrame:Lerp(goal, 0.35)
		end
	end

	if espEnabled then
		local current = {}
		local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
		for _, player in pairs(Players:GetPlayers()) do
			if isEnemy(player) and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
				local hrp = player.Character.HumanoidRootPart
				local dist = myRoot and (myRoot.Position - hrp.Position).Magnitude or 999
				if dist < 500 then
					current[player] = true
					local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position + Vector3.new(0, 1.5, 0))
					if onScreen and pos.Z > 0 then
						if not espBoxes[player] then
							espBoxes[player] = Drawing.new("Square")
							espBoxes[player].Thickness = 1.6
							espBoxes[player].Filled = false
							espTracers[player] = Drawing.new("Line")
							espTracers[player].Thickness = 1.4
						end
						local size = math.clamp(1700 / pos.Z, 16, 50)
						local visible = isVisible(player.Character)
						local color = visible and Color3.fromRGB(0, 255, 80) or Color3.fromRGB(255, 45, 45)

						espBoxes[player].Size = Vector2.new(size, size * 1.65)
						espBoxes[player].Position = Vector2.new(pos.X - size/2, pos.Y - size * 0.85)
						espBoxes[player].Color = color
						espBoxes[player].Visible = true

						espTracers[player].From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y)
						espTracers[player].To = Vector2.new(pos.X, pos.Y + size * 0.25)
						espTracers[player].Color = color
						espTracers[player].Visible = true
					end
				end
			end
		end
		for player, box in pairs(espBoxes) do
			if not current[player] then
				pcall(function() box:Remove() end)
				espBoxes[player] = nil
				if espTracers[player] then
					pcall(function() espTracers[player]:Remove() end)
					espTracers[player] = nil
				end
			end
		end
	end
end)

RunService.Heartbeat:Connect(function()
	local char = LocalPlayer.Character
	if not char then return end

	if char:FindFirstChild("Humanoid") then
		char.Humanoid.WalkSpeed = currentSpeed
	end

	if noclipEnabled then
		for _, p in pairs(char:GetDescendants()) do
			if p:IsA("BasePart") then p.CanCollide = false end
		end
	end

	if hitboxEnabled then
		local myRoot = char:FindFirstChild("HumanoidRootPart")
		for _, player in pairs(Players:GetPlayers()) do
			if isEnemy(player) and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
				local root = player.Character.HumanoidRootPart
				local dist = myRoot and (myRoot.Position - root.Position).Magnitude or 999

				if dist <= hitboxDistance then
					if not originalSizes[player] then
						originalSizes[player] = {
							root = root.Size,
							rootTrans = root.Transparency
						}
					end
					-- Solo agranda el RootPart, NO toca la cabeza
					root.Size = Vector3.new(hitboxSize, hitboxSize * 1.15, hitboxSize)
					root.Transparency = hitboxTransparency
					root.Color = hitboxColor
					root.Material = Enum.Material.ForceField
					root.CanCollide = false
				else
					if originalSizes[player] then
						pcall(function()
							root.Size = originalSizes[player].root
							root.Transparency = originalSizes[player].rootTrans or 0
							root.Color = Color3.fromRGB(163, 162, 165)
							root.Material = Enum.Material.Plastic
						end)
						originalSizes[player] = nil
					end
				end
			end
		end
	end

	if autoStickPlayer and autoStickPlayer.Character and autoStickPlayer.Character:FindFirstChild("HumanoidRootPart") then
		if char:FindFirstChild("HumanoidRootPart") then
			char.HumanoidRootPart.CFrame = autoStickPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3.5)
		end
	end
end)

UserInputService.JumpRequest:Connect(function()
	if infiniteJumpEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
		LocalPlayer.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
	end
end)

print("✅ TikTok alex@11 - Corregido")
